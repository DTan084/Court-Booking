// TODO: Axios instance
// - baseURL = process.env.NEXT_PUBLIC_API_URL
// - request interceptor: Bearer token từ zustand
// - response interceptor: 401 → refresh token → retry

// import axios from 'axios';

// const api = axios.create({
//   baseURL: process.env.NEXT_PUBLIC_API_URL,
//   headers: { 'Content-Type': 'application/json' },
// });

// TODO: Request interceptor — attach Authorization header
// TODO: Response interceptor — handle 401 refresh

// export { api };
